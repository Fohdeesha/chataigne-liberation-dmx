{
	"name": "Liberation DMX",
	"type": "DMX",
	"path": "Software",
	"version": "1.9.1",
	"description": "Drive Liberation laser software through its DMX Input system over Art-Net. Up to 8 zones, each mapped to one Liberation DMX Input profile (Basic 16ch or Extended 32ch). Gobo Bank / Gobo Select are hidden behind a single Clip dropdown listing the whole deck by Liberation's own clip numbers, or typed straight in as a Clip X / Clip Y pair; the Select Clip command offers both, a Stop clips mode, and a Duration after which the clip is stopped again. Position is a normalized Point2D written to the 16-bit coarse/fine channel pairs.",
	"url": "https://github.com/Fohdeesha/chataigne-liberation-dmx",
	"downloadURL": "https://github.com/Fohdeesha/chataigne-liberation-dmx/archive/refs/heads/master.zip",

	"hasInput": false,
	"hasOutput": true,
	"hideDefaultCommands": true,
	"hideDefaultParameters": [ "dmxType", "sendRate", "useMulticast", "autoAdd", "sendOnChangeOnly", "artNet/input", "pass_through" ],

	"defaults":
	{
		"dmxType": "Art-Net",
		"sendRate": 44,
		"sendOnChangeOnly": false,
		"autoAdd": false
	},

	"scripts":
	[
		"liberationDMX.js"
	],

	"parameters":
	{
		"Setup":
		{
			"type": "Container",
			"Zone Count":
			{
				"type": "Integer",
				"min": 0,
				"max": 8,
				"default": 1,
				"description": "How many Liberation zones to control. Each zone gets its own channel block and its own DMX Input profile row in Liberation. Lowering this blanks the removed zones' channels first, so a zone that was armed is disarmed rather than left streaming."
			},
			"New Zone Profile":
			{
				"type": "Enum",
				"options":
				{
					"Extended 32ch": 32,
					"Basic 16ch": 16
				},
				"description": "Profile given to zones as they are created. Each zone's own Profile parameter can be changed afterwards."
			},
			"Deck Columns":
			{
				"type": "Integer",
				"min": 1,
				"max": 2048,
				"default": 96,
				"description": "How wide your Liberation clip deck is, in columns. Only sizes each zone's Clip list (and the Clip list of Select Clip consequences created afterwards), so keep it just past the rightmost column you use. Liberation's own factory project is 89 columns wide. Lowering it past a zone's current clip clears that zone's clip."
			},
			"Auto Address":
			{
				"type": "Boolean",
				"default": true,
				"description": "Stack zones automatically from Base Universe / Base Address, using each zone's profile size (16 or 32 channels). Turn off to set Universe and Start Address per zone by hand."
			},
			"Base Universe":
			{
				"type": "Integer",
				"min": 1,
				"max": 4096,
				"default": 1,
				"description": "Universe of the first zone, in Liberation's 1-based UI numbering. Universe 1 = Art-Net Port-Address 0. Only used when Auto Address is on."
			},
			"Base Address":
			{
				"type": "Integer",
				"min": 1,
				"max": 512,
				"default": 1,
				"description": "Start channel of the first zone (1-512). Only used when Auto Address is on."
			},
			"Rebuild Zones":
			{
				"type": "Trigger",
				"description": "Recreate the zone tree and re-send every zone. Use after editing addresses by hand, or if the tree ever looks out of sync."
			},
			"Log Addressing":
			{
				"type": "Trigger",
				"description": "Re-check the addressing, refresh Address Map below and print it to the Logger panel (View > Logger)."
			},
			"Address Map":
			{
				"type": "String",
				"default": "",
				"readOnly": true,
				"multiline": true,
				"description": "Universe, channel range and profile of every zone, as they must be entered in Liberation's DMX Input window. Kept current, with any addressing problem listed underneath."
			}
		},
		"Global":
		{
			"type": "Container",
			"Arm Follows Clip":
			{
				"type": "Boolean",
				"default": true,
				"description": "Selecting a clip arms that zone automatically, and selecting 'None' disarms it, so a trigger or mapping only has to set the clip. SAFETY: with this on, choosing a clip immediately enables laser output for the zone. Turn it off to drive the Arm channel yourself."
			},
			"Intensity Follows Clip":
			{
				"type": "Boolean",
				"default": true,
				"description": "Selecting a clip also sets that zone's Intensity to full (255), so a clip never lands on a zone that was left dimmed."
			},
			"Master Intensity":
			{
				"type": "Float",
				"min": 0,
				"max": 1,
				"default": 1,
				"ui": "slider",
				"description": "Grand master: scales every zone's Intensity on the way out to DMX. Does not change the per-zone Intensity values, so pulling it down and back up restores the look."
			}
		}
	},

	"commands":
	{
		"Set Arm":
		{
			"menu": "Zone",
			"callback": "cmdSetArm",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"On": { "type": "Boolean", "default": true, "mappingIndex": 0, "description": "Arm channel: on sends 255 (output enabled), off sends 0." }
			}
		},
		"Set Intensity":
		{
			"menu": "Zone",
			"callback": "cmdSetIntensity",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": 0, "max": 1, "default": 1, "mappingIndex": 0, "description": "0-1 mapped to 0-100% brightness." }
			}
		},
		"Set Enabled":
		{
			"menu": "Zone",
			"callback": "cmdSetEnabled",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"On": { "type": "Boolean", "default": true, "mappingIndex": 0, "description": "A disabled zone keeps streaming but forces its Arm channel to 0, so Liberation stops rendering it." }
			}
		},
		"Blackout Zone":
		{
			"menu": "Zone",
			"callback": "cmdBlackout",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 0, "description": "Zone to black out. 0 = all zones. Sets Arm off; the rest of the zone's state is left untouched." }
			}
		},
		"Reset Zone":
		{
			"menu": "Zone",
			"callback": "cmdResetZone",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to reset to Liberation's recommended defaults (Arm off, intensity 1, white, centred, no clip). 0 = all zones." }
			}
		},

		"Set Master Intensity":
		{
			"menu": "Global",
			"callback": "cmdSetMasterIntensity",
			"parameters":
			{
				"Value": { "type": "Float", "min": 0, "max": 1, "default": 1, "mappingIndex": 0, "description": "Grand master applied to every zone's Intensity on the way out." }
			}
		},

		"Select Clip":
		{
			"menu": "Clip",
			"callback": "cmdSelectClip",
			"setupCallback": "setupSelectClipCommand",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Mode":
				{
					"type": "Enum",
					"options":
					{
						"Clip number": "number",
						"Clip list": "list",
						"Stop clips": "stop"
					},
					"description": "Clip number: type the X / Y pair Liberation prints in a clip's settings header. Clip list: pick the clip from a list of the whole deck. Stop clips: clear whatever the zone is playing, and disarm it when Arm Follows Clip is on."
				},
				"X":
				{
					"type": "Integer", "min": -1, "max": 2047, "default": 0,
					"description": "First figure of Liberation's clip number: deck column, 0 = leftmost. Right-click a clip in Liberation and its settings header prints the pair. Clamped to Setup > Deck Columns.",
					"dependency": { "source": "mode", "value": "number", "check": "equals", "action": "show" }
				},
				"Y":
				{
					"type": "Integer", "min": -1, "max": 4, "default": 0,
					"description": "Second figure of Liberation's clip number: deck row, 0 = top.",
					"dependency": { "source": "mode", "value": "number", "check": "equals", "action": "show" }
				},
				"Clip":
				{
					"type": "Enum",
					"options": { "None (no clip)": 0 },
					"description": "Every clip on the deck, named the way Liberation names it (x-y). The list is sized by Setup > Deck Columns at the moment this consequence is created.",
					"dependency": { "source": "mode", "value": "list", "check": "equals", "action": "show" }
				},
				"Duration":
				{
					"type": "Float", "min": 0, "max": 3600, "default": 0, "ui": "time",
					"description": "How long the clip plays before the module stops it again (clears the clip, and disarms the zone when Arm Follows Clip is on), like the MIDI module's Full Note 'On Time'. 0 = hold until something else changes the clip. Selecting another clip, Stop clips, Clear Clip or Reset Zone on the same zone cancels a pending stop.",
					"dependency": { "source": "mode", "value": "stop", "check": "notEquals", "action": "show" }
				}
			}
		},
		"Clear Clip":
		{
			"menu": "Clip",
			"callback": "cmdClearClip",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to clear (Gobo Select 0 = no clip). 0 = all zones. Same as Select Clip in its Stop clips mode." }
			}
		},

		"Set Colour":
		{
			"menu": "Colour",
			"callback": "cmdSetColour",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Colour": { "type": "Color", "default": [1, 1, 1, 1], "mappingIndex": 0, "description": "Desk RGB. Alpha is ignored - use Intensity for brightness." }
			}
		},
		"Set Colour Blend":
		{
			"menu": "Colour",
			"callback": "cmdSetColourBlend",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": 0, "max": 1, "default": 1, "mappingIndex": 0, "description": "0 = clip's own colour, 1 = desk RGB colour." }
			}
		},

		"Set Position":
		{
			"menu": "Transform",
			"callback": "cmdSetPosition",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Position": { "type": "Point2D", "min": [-1, -1], "max": [1, 1], "default": [0, 0], "mappingIndex": 0, "description": "-1..1 on both axes, (0,0) = centre. +X right, +Y DOWN (Liberation's own convention). Written to the 16-bit coarse/fine channel pairs." }
			}
		},
		"Set Position X":
		{
			"menu": "Transform",
			"callback": "cmdSetPositionX",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": -1, "max": 1, "default": 0, "mappingIndex": 0, "description": "-1 = full left, 0 = centre, 1 = full right. 16-bit." }
			}
		},
		"Set Position Y":
		{
			"menu": "Transform",
			"callback": "cmdSetPositionY",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": -1, "max": 1, "default": 0, "mappingIndex": 0, "description": "-1 = full up, 0 = centre, 1 = full down (+Y is down). 16-bit." }
			}
		},
		"Set Scale":
		{
			"menu": "Transform",
			"callback": "cmdSetScale",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Scale": { "type": "Point2D", "min": [-1, -1], "max": [1, 1], "default": [1, 1], "mappingIndex": 0, "description": "Size on each axis: 1 = 100%, the clip as authored (DMX 255). 0 = 0%, nothing renders (DMX 128). -1 = 100% mirrored on that axis (DMX 0)." }
			}
		},
		"Set Scale X":
		{
			"menu": "Transform",
			"callback": "cmdSetScaleX",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": -1, "max": 1, "default": 1, "mappingIndex": 0, "description": "1 = 100%, the clip as authored. 0 = 0%, nothing renders. -1 = 100% mirrored on this axis." }
			}
		},
		"Set Scale Y":
		{
			"menu": "Transform",
			"callback": "cmdSetScaleY",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": -1, "max": 1, "default": 1, "mappingIndex": 0, "description": "1 = 100%, the clip as authored. 0 = 0%, nothing renders. -1 = 100% mirrored on this axis." }
			}
		},
		"Set Zoom":
		{
			"menu": "Transform",
			"callback": "cmdSetZoom",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": 0, "max": 1, "default": 1, "mappingIndex": 0, "description": "0 = collapsed, 1 = normal size." }
			}
		},
		"Set Rotation":
		{
			"menu": "Transform",
			"callback": "cmdSetRotation",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones." },
				"Value": { "type": "Float", "min": -1, "max": 1, "default": 0, "mappingIndex": 0, "description": "Spin speed: -1 = max counter-clockwise, 0 = stopped, 1 = max clockwise." }
			}
		},

		"Set FX Level":
		{
			"menu": "Effects",
			"callback": "cmdSetFXLevel",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones. Extended 32ch profile only." },
				"FX": { "type": "Integer", "min": 1, "max": 4, "default": 1, "description": "Effect slot 1-4 (Liberation's first four effects)." },
				"Value": { "type": "Float", "min": 0, "max": 1, "default": 0, "mappingIndex": 0, "description": "Effect depth / amount." }
			}
		},
		"Set FX Parameter":
		{
			"menu": "Effects",
			"callback": "cmdSetFXParameter",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones. Extended 32ch profile only." },
				"FX": { "type": "Integer", "min": 1, "max": 4, "default": 1, "description": "Effect slot 1-4." },
				"Parameter": { "type": "Integer", "min": 1, "max": 2, "default": 1, "description": "Which of the effect's first two exposed parameters to set." },
				"Value": { "type": "Float", "min": 0, "max": 1, "default": 0.5, "mappingIndex": 0, "description": "0-1 mapped to 0-255. 0.5 is the recommended neutral value (DMX 128)." }
			}
		},
		"Clear Effects":
		{
			"menu": "Effects",
			"callback": "cmdClearEffects",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 0, "description": "Zone whose four FX levels go to 0 (parameters are left as they are). 0 = all zones." }
			}
		},

		"Set Tempo Override":
		{
			"menu": "Tempo",
			"callback": "cmdSetTempoOverride",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 1, "description": "Zone to target. 0 = all zones. Extended 32ch profile only." },
				"BPM": { "type": "Float", "min": 1, "max": 255, "default": 120, "mappingIndex": 0, "description": "Tempo for this zone's DMX-rendered content. Sent as coarse BPM + 1/256 fine." }
			}
		},
		"Clear Tempo Override":
		{
			"menu": "Tempo",
			"callback": "cmdClearTempoOverride",
			"parameters":
			{
				"Zone": { "type": "Integer", "min": 0, "max": 8, "default": 0, "description": "Zone that goes back to following Liberation's own tempo. 0 = all zones." }
			}
		}
	}
}
